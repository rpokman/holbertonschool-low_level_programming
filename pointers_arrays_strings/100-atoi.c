#include "main.h"

/**
 * _atoi - function
 *
 *@str: string
 */
int _atoi(char *s)
{
	int a = 0;
	int sign = 1;
	int result = 0;

	for (a = 0; s[a] != '\0'; a++)
	{
		if (s[a] >= '0' && s[a] <= '9')

	}
	return result * sign;
}
